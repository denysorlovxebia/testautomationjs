# Playwright Test Automation Framework

## Overview

This project contains automated end-to-end tests built with Playwright for the Practice Software Testing application.

The main goal is to demonstrate a clean and maintainable test automation setup using modern best practices.

---

## Getting Started

### 1. Install dependencies

```bash
npm install
```

### 2. Install Playwright browsers

```bash
npx playwright install
```

### 3. Run tests

```bash
npx playwright test
```

---

## Running Tests

### Run all tests

```bash
npx playwright test
```

### Run tests in UI mode

```bash
npx playwright test --ui
```

### Run tests in headed mode

```bash
npx playwright test --headed
```

### Run specific test file

```bash
npx playwright test tests/login.spec.ts
```

---

## 📊 Reports

### Open HTML report

```bash
npx playwright show-report
```

### Reporters enabled

- html
- dot
- json -> `test-results/results.json`
- ReportPortal (enabled automatically when RP env vars are set)

### ReportPortal Integration (optional)

1. Install runtime for ReportPortal stack:
- Docker Desktop, or
- Rancher Desktop (if company policy blocks Docker Desktop)

2. Start ReportPortal using official quick start from ReportPortal docs.

3. Configure env vars locally in `.env` (see `.env.example`):

```bash
RP_ENDPOINT=https://your-reportportal-host/api/v2
RP_PROJECT=your-project
RP_API_KEY=your-api-key
RP_LAUNCH=local-playwright-run
```

4. Run tests normally:

```bash
npx playwright test
```

When RP variables are present, Playwright sends launch and test results to ReportPortal in addition to local reports.

---

## Configuration

* Base URL is configured in `playwright.config.ts`
* Tests use relative paths (e.g. `/auth/login`)
* Custom test attributes are supported via:

```ts
testIdAttribute: 'data-test'
```

---

## Project Structure

```
tests/
  login.spec.ts

playwright.config.ts
tsconfig.json
package.json
```

---

##  Example Test Scenario

* Open login page
* Enter valid credentials
* Submit login form
* Verify successful login by:

  * checking URL
  * verifying page title
  * validating user name in navigation

---

## Notes

* Project uses Playwright only (no WDIO or other frameworks)
* Clean code approach is applied
* Unused files and autogenerated tests were removed

---

## Author

Created as part of test automation learning and practice.
